{
  "version": 2,
  "installCommand": "npm ci && npm run build -w shared && npm run build -w database",
  "builds": [
    {
      "src": "backend/api/index.ts",
      "use": "@vercel/node@latest"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/backend/api/index.ts"
    }
  ]
}
