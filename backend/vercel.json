{
  "version": 2,
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build -w shared && npm run prisma:generate -w database && npm run build -w backend",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node@latest"
    }
  ],
  "routes": [
    {
      "src": "/health/db",
      "dest": "/api/index.ts"
    },
    {
      "src": "/health",
      "dest": "/api/index.ts"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
